# Google OAuth / Gmail import (set only when the integration is enabled)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

# 32-byte or longer key used to encrypt OAuth tokens at rest
TOKEN_ENCRYPTION_KEY=

# Trusted canonical origin used for absolute Open Graph URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3000
